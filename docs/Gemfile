# Local development only — GitHub Pages installs this environment itself.
#   bundle install
#   bundle exec jekyll serve --livereload
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
